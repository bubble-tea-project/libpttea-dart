name: libpttea
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  logging: ^1.3.0
  # path: ^1.8.0
  ansiparser:
    path: ../../ansiparser-dart/ansiparser
  big5_utf8_converter: ^1.1.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0

